/**
 * @format
 */

import { AppRegistry } from 'react-native'
import { name as appName } from './app.json'
import ReactNativePlayground from './src/app'

AppRegistry.registerComponent(appName, () => ReactNativePlayground)
